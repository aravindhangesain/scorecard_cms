---
const heading = `Go beyond model types.
Procure PV modules with
top-performing bills of
materials.`;
---

<section class="hero-bom">
  <!-- Left Content -->
  <div class="hero-content">
    <h1>
      Go beyond model types.<br />
      Procure PV modules with<br />
      top-performing bills of<br />
      materials.
    </h1>

    <p>Join PVEL’s downstream partner network.</p>

    <a href="#" class="hero-btn">Sign Up</a>
  </div>

  <!-- Right Image -->
  <div class="hero-image">
    <img
      src="/images/2022/join-us.png"
      alt="PV Module Solar Cells"
    />
  </div>
</section>

<style>
    .hero-bom {
  display: grid;
  grid-template-columns: 1.05fr 1fr;
  min-height: 100vh;
  width: 100%;
  background: #1f2740;
}

/* LEFT */
.hero-content {
  padding: 120px 90px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  background: linear-gradient(180deg, #1f2740 0%, #232c4d 100%);
}

.hero-content h1 {
  font-family: "Georgia", serif;
  font-size: 32px;
  line-height: 1.25;
  font-weight: 500;
  color: #ffffff;
  margin-bottom: 30px;
}

.hero-content p {
  font-family: Arial, sans-serif;
  font-size: 16px;
  color: #e6e9f2;
  margin-bottom: 36px;
}

/* BUTTON */
.hero-btn {
  display: inline-block;
  background: #7bb36a;
  color: #ffffff;
  padding: 12px 26px;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 600;
  width: fit-content;
  text-decoration: none;
}

/* RIGHT */
.hero-image {
  position: relative;
  overflow: hidden;
}

.hero-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Responsive */
@media (max-width: 1024px) {
  .hero-bom {
    grid-template-columns: 1fr;
  }

  .hero-content {
    padding: 80px 40px;
  }

  .hero-image {
    height: 420px;
  }
}

@media (max-width: 600px) {
  .hero-content h1 {
    font-size: 38px;
  }
}

</style>