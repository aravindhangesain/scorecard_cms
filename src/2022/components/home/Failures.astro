<section class="failures">
  <div class="container">

    <!-- HEADER ROW -->
    <div class="failures-header">
      <h2 class="failures-title">Failures</h2>

      <a href="/failures" class="failures-btn">
        Learn More
      </a>
    </div>

    <div class="divider"></div>

    <!-- BODY -->
    <div class="failures-body">
      <!-- Image -->
      <div class="failures-image">
        <img
          src="/images/2022/VMS14849-2-1-1.png"
          alt="PV module failure"
        />
      </div>

      <!-- Content -->
      <div class="failures-text">
        <p>
          In a year of successes, PVEL also observed failures in testing for the
          PV Module Product Qualification Program. While fewer manufacturers
          experienced a failure this year as compared with the 2021 Scorecard,
          one quarter of BOMs suffered at least one failure during testing.
        </p>

        <p>
          The greatest number of failures occurred during the damp heat sequence.
          Explore 2022 failure data to learn more.
        </p>
      </div>
    </div>

  </div>
</section>


<style>
.failures {
  background: #f5f7fc;
  padding: 5.5rem 0;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}

/* ===== HEADER ===== */
.failures-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.failures-title {
  font-size: 2.4rem;
  font-weight: 600;
  margin: 0;
}

.failures-btn {
  background: #5CB66A;
  color: #fff;
  padding: 0.65rem 1.4rem;
  border-radius: 8px;
  font-size: 0.9rem;
  text-decoration: none;
  white-space: nowrap;
}

/* Divider line under header */
.divider {
  margin: 2rem 0 3.5rem;
  height: 1px;
  background: #e3e6ef;
}

/* ===== BODY ===== */
.failures-body {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 4rem;
  align-items: center;
}

.failures-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 14px;
}

.failures-text p {
  font-size: 1rem;
  line-height: 1.75;
  color: #2f2f2f;
  margin-bottom: 1.6rem;
}

/* ===== MOBILE ===== */
@media (max-width: 900px) {
  .failures-body {
    grid-template-columns: 1fr;
  }

  .failures-header {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }
}

</style>