---
import { getLang } from "../../../i18n/useLang";
import en from "../../../i18n/ln.json"; // English
import zh from "../../../i18n/ln-eg.json"; // Chinese

const lang = getLang(Astro);
const data = lang === "zh" ? zh : en;
const isZh = lang === "zh";
---

<section id="PowerDegration" class="dh-boms">
  <div class="dh-boms-container">
    <!-- LEFT SIDE -->
    <div class="dh-boms-left">
      <h2>
        {data.dhpowertitle_line1}<br />
        <span>{data.dhpowertitle_line2}</span>
      </h2>

      <div class="chart-wrapper">
        <img
          src="/images/Power-Degradation-of-DH-BOMs.jpg"
          alt={data.dhpowerleftimage_alt}
          class="modal-trigger"
          title={data.dhpowerleftimage_title}
        />
      </div>
    </div>

    <!-- RIGHT SIDE -->
    <a
      href={isZh
        ? "/zh/top-performers/?pqp_test=DH"
        : "/top-performers/?pqp_test=DH"}
      class="dh-boms-link"
    >
      <div class="dh-boms-right">
        <img
          class="badge-img"
          src="/images/Kiwa-PVEL-2025-TP-Mark-2.svg"
          alt={data.dhpowerrightimage_alt}
        />

        <div class="cta">
          <h4>
            {data.dhpowercta_title1}<br />
            {data.dhpowercta_title2}
          </h4>
          <p>{data.dhpowercta_description}</p>
        </div>
      </div>
    </a>

    <div>
      <p class="footnote-left">
        {data.dhpowerfootnotes_text1}
      </p>

      <p class="footnote-left1">
        {data.dhpowerfootnotes_text2}
      </p>

      <a href={data.dhpowertext_link_url} target="_blank" class="text-link">
        {data.dhpowertext_link}
      </a>
    </div>
    <div class="footnote-row">
      <a
        class="footnote"
        href={data.dhpowerfootnoterow_texturl}
        target="_blank"
      >
        {data.dhpowerfootnoterow_text}
      </a>

      <img class="cta-arrow" src="/images/Icon_Arrow_Black.svg" />
    </div>
  </div>
</section>

<style>
  .dh-boms {
    padding: clamp(3rem, 6vw, 5rem) 1.5rem;
    background: #ffffff;
  }

  .dh-boms-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 2.2fr 0.8fr;
    gap: clamp(2rem, 1vw, 5rem);
    align-items: start;
  }

  .dh-boms-left h2 {
    font-size: 46.8px;
    line-height: 57.8px;
    color: #072643;
    margin-bottom: 2rem;
    font-family: "Azo Sans" sans-serif;
    font-weight: 400;
    text-align: left;
    letter-spacing: -1.5px;
  }

  .dh-boms-left h2 span {
    font-weight: 800;
    font-family: "Azo Sans" sans-serif;
    font-style: italic;
  }

  .chart-wrapper img {
    width: 100%;
    display: block;
  }
  .dh-boms-link {
    display: contents;
    text-decoration: none;
    color: inherit;
  }

  /* Footnotes */
  .footnote-left {
    font-size: 12px;
    color: #072643;
    line-height: 21px;
    font-family: "Work Sans", sans-serif;
    width: 80%;
  }
  .footnote-left1 {
    font-size: 12px;
    color: #072643;
    line-height: 21px;
    font-family: "Work Sans", sans-serif;
    width: 80%;
    margin-top: 40px;
  }

  .footnote-row {
    grid-column: 1 / -1;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .footnote-row .footnote {
    font-size: 14px;
    max-width: 85%;
    line-height: 18px;
    letter-spacing: normal;
    font-family: "Work Sans", sans-serif;
    color: #072643;
    text-decoration: none;
    position: relative;
    display: inline-block;
    text-align: left;
  }

  .dh-boms-right {
    display: flex;
    flex-direction: column;
    align-items: center;

    margin-top: 190px;
  }

  .badge-img {
    width: clamp(180px, 25vw, 240px);
    height: auto;
    /* margin-bottom: 2rem; */
  }

  .cta h4 {
    font-size: 24px;
    color: #282828;
    letter-spacing: -1.5px;
    margin-bottom: 1rem;
    line-height: 32px;
    font-family: "Azo Sans", sans-serif;
    font-weight: 400;
    text-align: center;
  }

  .cta p {
    font-size: 16px;
    color: #282828;
    font-weight: 400;
    max-width: 200px;
    line-height: 28px;
    font-family: "Work Sans", sans-serif;
    text-align: center;
  }

  .text-link {
    color: #072643;
    text-decoration: none;
    font-style: italic;
    font-weight: 400;
    font-size: 14px;
    font-family: "Work Sans", sans-serif;
    margin-top: 20px;
    padding-bottom: 4px;
    line-height: 21px;
    /* underline using background */
    background-image: linear-gradient(#072643, #072643);
    background-repeat: no-repeat;
    background-size: 100% 1.5px;
    background-position: 100% 100%;

    transition: background-size 0.4s ease;
  }

  /* hover: underline disappears left → right */
  .text-link:hover {
    background-size: 0% 1.5px;
  }

  /* Keep arrow size fixed */
  .footnote-row img.cta-arrow {
    width: 60px; /* keep original size */
    height: auto;
    transition: transform 0.3s ease;
  }

  /* Slide on hover */
  .footnote-row:hover img.cta-arrow {
    transform: translateX(8px);
  }

  /* Click feedback */
  .footnote-row:active img.cta-arrow {
    transform: translateX(12px);
  }

  @keyframes arrowSlide {
    0% {
      transform: translateX(0);
    }
    60% {
      transform: translateX(12px);
    }
    100% {
      transform: translateX(8px);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footnote-row img.cta-arrow {
      transition: none;
      animation: none;
    }
  }

  @media (max-width: 1024px) {
    .dh-boms-container {
      grid-template-columns: 1fr;
    }

    .dh-boms-right {
      margin-top: 3rem;
    }

    .footnote-row .footnote {
      max-width: 100%;
    }
  }

  @media (max-width: 768px) {
    .dh-boms {
      padding: 3rem 1.25rem;
    }

    .dh-boms-left h2 {
      text-align: left;
    }

    .dh-boms-right {
      margin-top: 2.5rem;
    }

    .cta p {
      max-width: 100%;
    }

    .footnote-row {
      flex-direction: column;
      align-items: flex-start;
    }

    .arrow-link {
      align-self: flex-end;
    }
  }

  @media (max-width: 480px) {
    .dh-boms-left h2 {
      font-size: 28px;
    }

    .badge-img {
      width: 160px;
    }
  }

  /* Cursor */
  .modal-trigger {
    cursor: pointer;
  }
</style>
